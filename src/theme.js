import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#192756",
    },
    secondary: {
      main: "#5a6978",
    },
  },
  text: {
    primary: "#1A2027",
    secondary: "#5a6978",
  },
});

export default theme;
